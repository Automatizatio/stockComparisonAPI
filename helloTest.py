# Import the logging module
import logging

# Set the basic configuration for logging
logging.basicConfig(level=logging.INFO)

# Use the logger to output a message
logging.info("Hello, this is a test message")
